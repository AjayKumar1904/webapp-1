pipeline {
  agent any
  tools {
  maven 'Maven'
  }
  stages {
    stage ('Initialize') {
      steps{
       sh 'PATH = ${PATH}'
       sh 'M2_HOME = ${M2_HOME}'
      }
    }
    stage ('Build') {
      steps {
        sh 'mvn clean package'
      }
    }
  }
}
